export type FormikErrorType = {
    email?: string;
    password?: string;
    rememberMe?: boolean;
};

export type FormikInitialValuesType = {
    email: string;
    password: string;
    rememberMe: boolean;
};
